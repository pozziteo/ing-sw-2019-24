package model.deck;

/**
 * PowerUpDeck is the subclass that represents a model.deck.Deck of PowerUp cards
 */

public class PowerUpDeck extends Deck{

    /**
     * Calls the constructor of the superclass
     */
    protected PowerUpDeck () {
        super();
    }
}
