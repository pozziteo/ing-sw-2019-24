package view;

public interface UserInterface {

    /**
     * Method to invoke some action on the controller based on what the player did
     */
    void callController(/*insert param*/);

    /**
     * Method to get a state to display from the model
     */
    void getState(/*insert param*/);

}
